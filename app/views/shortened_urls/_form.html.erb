<%= form_for [@user, @shortened_url] do |f| %>

<%= f.label :original_url %>
<%= f.text_field :original_url %>
<%= f.label :new_url %> (leave blank to generate random)
<%= f.text_field :new_url %>
<%= f.submit %>
<% end %>